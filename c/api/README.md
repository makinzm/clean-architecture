# How to build and listen to the server

```bash
gcc -o main.out src/main.c
./main.out

# and then Ctrl + C to stop the server
```

```bash
curl http://localhost:9999

curl http://localhost:9999/hello
```
